{
  "id": "tradle.DocumentValidityCheckFail",
  "title": "Reason for check override",
  "subClassOf": "tradle.Enum",
  "type": "tradle.Model",
  "properties": {
    "reasons": {
      "displayName": true,
      "type": "string"
    }
  },
  "enum": [
    { "id": "expired", "title": "Document expired" },
    { "id": "differentFromScanned", "title": "Different from the scanned document"}
  ]
}
