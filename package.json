{
  "name": "@tradle/models",
  "version": "4.10.9",
  "description": "models for tradle apps",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "./node_modules/.bin/jsonlint-cli `git ls-files models/*.json`",
    "validate": "node run merge && node ./validate -f ./models.js",
    "merge": "node merge.js ./models ./models.js",
    "postpublish": "git push -u"
  },
  "pre-commit": [
    "merge",
    "lint"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/tradle/models"
  },
  "keywords": [
    "tradle",
    "models"
  ],
  "author": "Mark Vayngrib <mark.vayngrib@lablz.com> (http://github.com/mvayngrib)",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/tradle/models/issues"
  },
  "homepage": "https://github.com/tradle/models",
  "devDependencies": {
    "git": "^0.1.5",
    "jsonlint-cli": "^1.0.1",
    "pre-commit": "^1.1.3"
  },
  "dependencies": {
    "minimist": "^1.2.0",
    "write-file-atomic": "^1.1.4"
  }
}
